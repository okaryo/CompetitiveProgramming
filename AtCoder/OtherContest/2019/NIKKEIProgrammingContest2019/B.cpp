#include <bits/stdc++.h>
using namespace std;

int main() {
  int n;
  cin >> n;
  vector<string> a(3);
  cin >> a.at(0) >> a.at(1) >> a.at(2);

  int ans = 300;
  for (int i = 0; i < n; i ++) {
    vector<int> b(26);
    for (int j = 0; j < 3; j ++) {
      
    }
  }

  cout << ans << endl;
}
